resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: myapp
    patch: |
      - op: add
        path: /spec/template/spec/containers/0/env
        value:
          - name: ENV
            value: DEV